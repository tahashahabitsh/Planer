package com.taha.planer.ui

/**
 * استایل کلی رابط کاربری اپ.
 * این enum فقط برای سوییچ بین سه سبک اصلی استفاده می‌شود.
 */
enum class DesignStyle {
    Glass,       // گلاسمورفیسم
    Neomorph,    // نئومورفیسم
    Illustration // سبک تصویرسازی
}
