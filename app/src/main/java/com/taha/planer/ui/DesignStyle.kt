package com.taha.planer.ui

/**
 * استایل کلی طراحی رابط کاربری.
 *
 * Glass       -> گلاسمورفیسم
 * Neomorph    -> نئومورفیسم
 * Illustration-> سبک تصویرسازی
 *
 * هر جا در کد از DesignStyle استفاده کرده‌ایم، به این enum اشاره می‌شود.
 */
enum class DesignStyle {
    Glass,
    Neomorph,
    Illustration
}
